import { RemovePasswordInterceptor } from './remove-password.interceptor';

describe('RemovePasswordInterceptor', () => {
  it('should be defined', () => {
    expect(new RemovePasswordInterceptor()).toBeDefined();
  });
});
